# Abhijay — Portfolio

Dynamic personal portfolio for college projects. Featured demos are hand-linked;
the repo list is fetched live from the GitHub API (`Abhijay360`).

## Local

```bash
npm install
npm run dev
```

## Deploy (Render static site)

```bash
npm run build
```

`render.yaml` publishes `dist/` on the free static plan.
